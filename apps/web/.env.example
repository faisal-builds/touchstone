# Backend service URLs the dashboard's BFF proxies to (server-side only).
CONTROL_PLANE_URL=http://localhost:8000
RHD_URL=http://localhost:8030
# Cookie signing/flags
SESSION_COOKIE_NAME=ts_session
NODE_ENV=development
