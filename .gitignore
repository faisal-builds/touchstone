# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
# Env / secrets
.env
.env.*
!.env.example
# Infra / local data
pgdata/
*.log
# OS / editor
.DS_Store
.idea/
.vscode/
# Node (web app)
node_modules/
.next/
# Terraform (local provider cache + state; infra is validated, never provisioned here)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
# Test/coverage artifacts (produced by CI; not committed)
reports/
coverage.xml
junit.xml
# Helm render output (produced by infra-validation / deploy-dryrun in the workspace)
rendered.yaml
